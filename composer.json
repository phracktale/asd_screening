{
    "name": "phracktale/tsa-repere",
    "description": "TSA-Repère — coeur metier de l'application de reperage et d'orientation du trouble du spectre de l'autisme (moteur de formulaire multi-age, psychometrie, structuration clinique).",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "TsaRepere\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "TsaRepere\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit"
    }
}
